services:
  - type: web
    name: copernicus-marine-visualiser
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server --bind 0.0.0.0:$PORT --timeout 300 --workers 2
    plan: free
